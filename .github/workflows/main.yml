name: Build Borderfree Feed

# 🔑 Give the GITHUB_TOKEN write access so it can push feed.xml
permissions:
  contents: write

on:
  push:
    branches: [ main ]
  workflow_dispatch: {}
  schedule:
    - cron: "0 */6 * * *" # every 6 hours (optional)

jobs:
  build:
    runs-on: ubuntu-latest

    # (optional) you can also set it here instead of top-level:
    # permissions:
    #   contents: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Build feed.xml
        run: npm run build:feed

      - name: Commit and push feed.xml
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add feed.xml
          if ! git diff --cached --quiet; then
            git commit -m "Update feed.xml"
            git push
          else
            echo "No changes to commit."
          fi